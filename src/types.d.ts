export interface PreviewImage {
  image: File;
  url: string;
}

export type TranslatedCssConfig = {
  twProps: string;
  cssProps: string;
};

export type Color = {
  name: string;
  value: string;
};
